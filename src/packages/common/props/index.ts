export const commonProps = {
    theme: {
        type: String,
        default: 'global'
    },
    disabled: {
        type: Boolean,
        default: false
    },
    lang: {
        type: String,
        default: "global"
    }
}